{
  "meta": {
    "name": "MoneyPrinterTurbo Script Candidates",
    "description": "Batch generate ready-to-use scripts and material search terms for MoneyPrinterTurbo from a video topic.",
    "scenario": "short-video-script",
    "inputSummary": "Video topic, generation requirements, and candidate number",
    "primaryOutputType": "text",
    "tags": ["MoneyPrinterTurbo", "Short Video", "Script Candidates"]
  },
  "steps": [
    {
      "stepId": "stp_script01",
      "displayName": "Generate Short-Video Script and Material Terms",
      "executionUnit": "text-generate",
      "instruction": "You are a short-video scriptwriter. Based on the input, write a complete spoken script that fits the target duration and provide 5 to 10 short English keywords for searching video materials. Different candidate numbers should use different angles. Output only one valid JSON object. Do not output Markdown, code fences, explanations, or any text outside JSON. The JSON must strictly follow this structure: {\"script\":\"complete script\",\"videoTerms\":[\"keyword one\",\"keyword two\"]}. Both script and videoTerms must be non-empty.",
      "defaultModelRef": {
        "modelKey": "google/gemini-2.5-flash"
      }
    }
  ],
  "inputSchema": {
    "instructions": [
      "Each row generates one independent script candidate.",
      "candidateIndex keeps multiple candidates for the same topic distinct."
    ],
    "fields": [
      {
        "key": "subject",
        "label": "Video Topic",
        "description": "The topic or key idea the video should cover",
        "valueType": "string",
        "required": true,
        "order": 1,
        "presentation": {
          "widget": "textarea",
          "placeholder": "e.g. how ordinary people can be more productive with AI"
        }
      },
      {
        "key": "requirements",
        "label": "Generation Requirements",
        "description": "Output language, target duration, style, and any additional requirements",
        "valueType": "string",
        "required": true,
        "order": 2,
        "presentation": {
          "widget": "textarea",
          "placeholder": "Output language: en-US\nTarget duration (seconds): 60\nStyle or extra requirements: create contrast at the beginning"
        }
      },
      {
        "key": "candidateIndex",
        "label": "Candidate Number",
        "description": "The candidate sequence starting from 1 within the same batch",
        "valueType": "string",
        "required": true,
        "order": 3
      }
    ],
    "sampleRows": [
      {
        "values": {
          "subject": "How ordinary people can be more productive with AI",
          "requirements": "Output language: en-US\nTarget duration (seconds): 60\nStyle or extra requirements: create contrast at the beginning and end with an actionable suggestion",
          "candidateIndex": "1"
        }
      }
    ]
  },
  "paramBindings": [
    {
      "stepId": "stp_script01",
      "paramKey": "prompt",
      "bindMode": "shared",
      "separator": "\n",
      "sources": [
        {"kind": "literal", "literal": "Video topic: "},
        {"kind": "field_ref", "fieldKey": "subject"},
        {"kind": "literal", "literal": "Generation requirements: "},
        {"kind": "field_ref", "fieldKey": "requirements"},
        {"kind": "literal", "literal": "Candidate number: "},
        {"kind": "field_ref", "fieldKey": "candidateIndex"}
      ]
    }
  ]
}
