# Run using a Claude subscription (Pro / Max / Team) as the LLM Provider.
#
# Differences from docker-compose.release.yml:
#   1. Builds Dockerfile.claude, which bundles the claude CLI in the image;
#   2. Mounts the whole project directory so the claude_code Provider code is live;
#   3. Injects CLAUDE_CODE_OAUTH_TOKEN. Containers have no keychain, so the CLI
#      can only authenticate with a long-lived token from `claude setup-token`.
#
# Export the token before starting:
#   export CLAUDE_CODE_OAUTH_TOKEN="$(claude setup-token)"
#   docker compose -f docker-compose.claude.yml up -d --build

x-common-build: &common-build
  context: .
  dockerfile: Dockerfile.claude

x-common-volumes: &common-volumes
  - ./:/MoneyPrinterTurbo

x-common-environment: &common-environment
  # Fail fast at compose time when the token is missing, instead of failing
  # later during generation after the container is already running.
  CLAUDE_CODE_OAUTH_TOKEN: ${CLAUDE_CODE_OAUTH_TOKEN:?run 'export CLAUDE_CODE_OAUTH_TOKEN="$(claude setup-token)"' first}

services:
  webui:
    build: *common-build
    container_name: "moneyprinterturbo-webui"
    ports:
      - "127.0.0.1:8501:8501"
    command: [ "streamlit", "run", "./webui/Main.py", "--server.address=0.0.0.0", "--server.port=8501", "--browser.serverAddress=127.0.0.1", "--server.enableCORS=True", "--browser.gatherUsageStats=False", "--client.toolbarMode=minimal", "--logger.hideWelcomeMessage=True", "--server.showEmailPrompt=False" ]
    volumes: *common-volumes
    environment: *common-environment
    restart: always
  api:
    build: *common-build
    container_name: "moneyprinterturbo-api"
    ports:
      - "127.0.0.1:8080:8080"
    command: [ "python3", "main.py" ]
    volumes: *common-volumes
    environment: *common-environment
    restart: always
